.container 
  .row
    .col
      h1 Orders

      table.table#orders
        tbody
          - @orders.each do |order|
            == render order
      - if current_user.manager?
        = link_to "New order", new_order_path, class: "btn btn-primary"
